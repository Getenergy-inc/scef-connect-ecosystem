import EduAidMasterTimeline from "@/pages/programs/EduAidMasterTimeline";

// Dedicated route /eduaid-africa-timeline reuses the full EduAid-Africa master timeline page.
const EduAidAfricaTimeline = () => <EduAidMasterTimeline />;

export default EduAidAfricaTimeline;
